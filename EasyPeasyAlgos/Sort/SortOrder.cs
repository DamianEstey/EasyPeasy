namespace EasyPeasyAlgos.Sort
{
    /// <summary>
    /// This enum is used to determine the sort order of the array.
    /// </summary>
    public enum SortOrder
    {
        Ascending,
        Descending
    }
}